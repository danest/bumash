
<% @products.each do |product| %>
	
	<div class ="images_picture">
	<%= link_to (image_tag(product.photo.url, :size => "366x480")), product_path(product.id), :method => :put, :title => "Vote for this image"  %>
	<div class="vote awesome">
	<%= link_to "Hotter", product_path(product.id), :method => :put, :class => "vote" %>
	</div>
	<div class ="clear"></div>
	</div>
	
<% end %>
<div class ="clear"></div>