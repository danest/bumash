<!DOCTYPE html>
<html>
  <head>
    <title>Binghamton University Face Mash</title>
    <%= csrf_meta_tag %>
    <%= render 'layouts/stylesheets' %>
	<%= javascript_include_tag :defaults %>
	<meta name="title" content="Binghamton University Face Mash - Rate and compare your classemates" />
	<meta name="description" content="Binghamton University Face Mash - Rate your classmates and see who is the hottest girl on campus" />
  </head>
  <body>
    <div class="container">
	<%= render 'layouts/header' %>
      <section class="round">
		<% flash.each do |key, value| %>
		<div class="flash <%= key %>">
			<%= value %>
		</div>
		<% end %>
        <%= yield %>
      </section>
		<h5>
			Contact <a href="mailto:bumashup@gmail.com"> bumashup@gmail.com </a>
		</h5>
		<%= debug(params) if Rails.env.development? %>
    </div>
  </body>
	<script type="text/javascript">

	  var _gaq = _gaq || [];
	  _gaq.push(['_setAccount', 'UA-17557178-3']);
	  _gaq.push(['_trackPageview']);

	  (function() {
	    var ga = document.createElement('script'); ga.type = 'text/javascript'; ga.async = true;
	    ga.src = ('https:' == document.location.protocol ? 'https://ssl' : 'http://www') + '.google-analytics.com/ga.js';
	    var s = document.getElementsByTagName('script')[0]; s.parentNode.insertBefore(ga, s);
	  })();

	</script>
</html>